# This workflow runs tests on push or pull requests
name: CI

on:
  # Triggers the workflow on push or pull request events but only for the master branch
  push:
      branches: [ master ]
  pull_request:
      branches: [ master ]


  workflow_dispatch:


jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Workspace
        uses: actions/checkout@v3

      - name: Install Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install pip and pip-tools
        run: python -m pip install --upgrade pip pip-tools

      - name: Generate requirements.txt file
        run: pip-compile requirements.in

      - name: Install dependencies
        run: pip-sync

      - name: Execute tests
        run: pytest

      - name: Check formatting
        run: autopep8 --aggressive --aggressive --max-line-length 120 --diff --recursive --exit-code advent

